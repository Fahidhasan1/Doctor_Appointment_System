@model Doctor_Appointment_System.Models.Receptionist

@{
    ViewData["Title"] = "Delete Receptionist";
    Layout = "_AdminLayout";
}

<section class="dashboard-main-inner">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h2 class="h4 mb-0 text-danger">Delete Receptionist</h2>
            <small class="text-muted">
                Are you sure you want to delete this receptionist? The user account will be deactivated.
            </small>
        </div>
        <a asp-action="Receptionists" class="btn btn-outline-secondary">
            ← Back to Receptionists
        </a>
    </div>

    <div class="card border-0 shadow-sm">
        <div class="card-body">
            <div class="alert alert-warning">
                <strong>Warning:</strong> This action cannot be undone. The receptionist record will be removed
                and the user will no longer be able to log in.
            </div>

            <dl class="row mb-4">
                <dt class="col-sm-3">Receptionist name</dt>
                <dd class="col-sm-9">@($"{Model.User.FirstName} {Model.User.LastName}")</dd>

                <dt class="col-sm-3">Email</dt>
                <dd class="col-sm-9">@Model.User.Email</dd>

                <dt class="col-sm-3">Phone</dt>
                <dd class="col-sm-9">@Model.User.PhoneNumber</dd>

                <dt class="col-sm-3">Desk No</dt>
                <dd class="col-sm-9">@Model.DeskNo</dd>
            </dl>

            <form asp-action="DeleteReceptionist" method="post">
                <input type="hidden" asp-for="Id" />
                <div class="d-flex justify-content-between">
                    <a asp-action="Receptionists" class="btn btn-outline-secondary">
                        Cancel
                    </a>
                    <button type="submit" class="btn btn-danger">
                        Yes, Delete Receptionist
                    </button>
                </div>
            </form>
        </div>
    </div>
</section>
