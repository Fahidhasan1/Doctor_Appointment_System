@* @using Doctor_Appointment_System.Models
@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@{
    // Read query string like /?auth=login or /?auth=register
    var openAuth = Context.Request.Query["auth"].ToString();
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Doctor Appointment System</title>

    <link rel="icon" type="image/png" href="~/images/favicon1.png" />

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- Site styles (landing + nav + modal) -->
    <link rel="stylesheet" href="~/css/home.css" asp-append-version="true" />

    @RenderSection("Styles", required: false)
</head>
<body class="body-wrapper">
    <!-- Helper flag so JS can auto-open modal based on ?auth= -->
    <div id="authOpenFlag" data-open-auth="@openAuth"></div>

    <!-- ===== Navigation ===== -->
    <nav class="main-navigation">
        <div class="nav-container">
            <a asp-controller="Home" asp-action="Index" class="site-logo">
                <span class="logo-icon">🏥</span>
                <span>HealthCare Appointments</span>
            </a>

            <button class="mobile-menu-toggle" id="mobileMenuBtn" aria-label="Toggle navigation">
                <span id="menuIcon">☰</span>
            </button>

            <div class="nav-right" id="navRight">
                <ul class="nav-menu-list" id="navMenu">
                    <li class="nav-menu-item">
                        <a asp-controller="Home" asp-action="Index" class="nav-link">Home</a>
                    </li>
                    <li class="nav-menu-item">
                        <a href="#about" class="nav-link">About</a>
                    </li>
                    <li class="nav-menu-item">
                        <a href="#contact" class="nav-link">Contact</a>
                    </li>
                </ul>

                <div class="search-wrapper">
                    <input type="text" class="search-input" placeholder="Search doctors, specialties..." />
                    <button class="search-button">Search</button>
                </div>

                <ul class="nav-auth-list">
                    @if (SignInManager.IsSignedIn(User))
                    {
                        <li class="nav-auth-item">
                            <!-- later we’ll route by role; for now goes to Home or a common dashboard -->
                            <a asp-controller="Home" asp-action="Index" class="auth-link">Dashboard</a>
                        </li>
                        <li class="nav-auth-item">
                            <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                                <button type="submit" class="auth-link auth-link-button" style="background:none;border:none;">
                                    Logout
                                </button>
                            </form>
                        </li>
                    }
                    else
                    {
                        <!-- These buttons open the popup modal -->
                        <li class="nav-auth-item">
                            <button type="button" class="auth-link auth-link-button" id="navLoginBtn">Login</button>
                        </li>
                        <li class="nav-auth-item">
                            <button type="button" class="auth-link auth-link-button" id="navRegisterBtn">Register</button>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </nav>

    <!-- ===== PAGE CONTENT ===== -->
    <main role="main">
        @RenderBody()
    </main>

    <!-- ===== AUTH MODAL OVERLAY (Login + Register) ===== -->
    <div id="authOverlay" class="auth-overlay hidden">
        <div class="auth-overlay-backdrop"></div>

        <div class="auth-modal-wrapper">
            <!-- LOGIN CARD -->
            <div class="auth-modal-card auth-login-card" id="loginModal">
                <button type="button" class="auth-close-btn" id="authCloseBtn">&times;</button>

                <h2 class="auth-title">Welcome Back</h2>
                <p class="auth-subtitle">Login to manage your doctor appointments</p>

                @if (TempData["LoginError"] != null)
                {
                    <div class="auth-alert auth-alert-error">
                        @TempData["LoginError"]
                    </div>
                }

                <form asp-controller="Account" asp-action="Login" method="post" novalidate>
                    <div class="mb-3">
                        <label class="form-label">Email address</label>
                        <input name="Email" type="email" class="form-control auth-input" placeholder="Enter email" />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Password</label>
                        <input name="Password" type="password" class="form-control auth-input" placeholder="Enter password" />
                    </div>

                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <div class="form-check">
                            <input name="RememberMe" class="form-check-input" type="checkbox" id="rememberMeCheckbox" />
                            <label class="form-check-label" for="rememberMeCheckbox">
                                Remember me
                            </label>
                        </div>
                        <a href="#" class="auth-link-inline">Forgot password?</a>
                    </div>

                    <button type="submit" class="auth-primary-btn">Login</button>

                    <div class="auth-bottom-text">
                        Don’t have an account?
                        <button type="button" class="auth-link-inline auth-switch-btn" data-target="register">
                            Register here
                        </button>
                    </div>
                </form>
            </div>

            <!-- REGISTER CARD -->
            <div class="auth-modal-card auth-register-card hidden" id="registerModal">
                <button type="button" class="auth-close-btn" id="authCloseBtn2">&times;</button>

                <h2 class="auth-title">Create Account</h2>
                <p class="auth-subtitle">Register to book and manage your doctor appointments</p>

                @if (TempData["RegisterError"] != null)
                {
                    <div class="auth-alert auth-alert-error">
                        @TempData["RegisterError"]
                    </div>
                }

                <form asp-controller="Account" asp-action="Register" method="post" novalidate>
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label">First name</label>
                            <input name="FirstName" class="form-control auth-input" placeholder="Enter first name" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Last name</label>
                            <input name="LastName" class="form-control auth-input" placeholder="Enter last name" />
                        </div>

                        <div class="col-12">
                            <label class="form-label">Email address</label>
                            <input name="Email" type="email" class="form-control auth-input" placeholder="example@domain.com" />
                            <small class="text-muted">Use a valid email. It will be used for login and notifications.</small>
                        </div>

                        <div class="col-12">
                            <label class="form-label">Mobile number</label>
                            <input name="PhoneNumber" class="form-control auth-input" placeholder="e.g. 01XXXXXXXXX" />
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Date of birth</label>
                            <input name="DateOfBirth" type="date" class="form-control auth-input" />
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Gender</label>
                            <select name="Gender" class="form-select auth-input">
                                <option value="">Select gender</option>
                                <option>Male</option>
                                <option>Female</option>
                                <option>Other</option>
                            </select>
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Password</label>
                            <input name="Password" type="password" class="form-control auth-input" placeholder="Enter password" />
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Confirm password</label>
                            <input name="ConfirmPassword" type="password" class="form-control auth-input" placeholder="Confirm password" />
                        </div>

                        <div class="col-12">
                            <div class="form-check">
                                <input name="AcceptTerms" type="checkbox" class="form-check-input" id="acceptTermsCheckbox" />
                                <label class="form-check-label" for="acceptTermsCheckbox">
                                    I agree to the terms &amp; conditions and privacy policy.
                                </label>
                            </div>
                        </div>

                        <div class="col-12">
                            <button type="submit" class="auth-primary-btn">Create Account</button>
                        </div>
                    </div>

                    <div class="auth-bottom-text">
                        Already have an account?
                        <button type="button" class="auth-link-inline auth-switch-btn" data-target="login">
                            Login here
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- ===== Footer ===== -->
    <footer class="footer-section">
        <div class="footer-content">
            <h3 class="footer-title">🏥 HealthCare Appointments</h3>
            <p class="footer-description">Booking appointments with Sunshine Hospital made simple.</p>
            <p class="footer-copyright">
                &copy; @DateTime.UtcNow.Year Doctor Appointment System. All rights reserved.<br />
                Designed for responsive viewing on mobile, tablet and desktop.
            </p>
        </div>
    </footer>

    <!-- Bootstrap JS bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Site JS -->
    <script src="~/js/home.js" asp-append-version="true"></script>

    @RenderSection("Scripts", required: false)
</body>
</html> *@


@using Doctor_Appointment_System.Models
@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@{
    // Read query string like /?auth=login or /?auth=register
    var openAuth = Context.Request.Query["auth"].ToString();
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Doctor Appointment System</title>

    <link rel="icon" type="image/png" href="~/images/favicon1.png" />

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- Site styles (landing + nav + modal) -->
    <link rel="stylesheet" href="~/css/home.css" asp-append-version="true" />

    @RenderSection("Styles", required: false)
</head>
<body class="body-wrapper">
    <!-- Helper flag so JS can auto-open modal based on ?auth= -->
    <div id="authOpenFlag" data-open-auth="@openAuth"></div>

    <!-- ===== Navigation ===== -->
    <nav class="main-navigation">
        <div class="nav-container">
            <a asp-controller="Home" asp-action="Index" class="site-logo">
                <span class="logo-icon">🏥</span>
                <span>HealthCare Appointments</span>
            </a>

            <button class="mobile-menu-toggle" id="mobileMenuBtn" aria-label="Toggle navigation">
                <span id="menuIcon">☰</span>
            </button>

            <div class="nav-right" id="navRight">
                <ul class="nav-menu-list" id="navMenu">
                    <li class="nav-menu-item">
                        <a asp-controller="Home" asp-action="Index" class="nav-link">Home</a>
                    </li>
                    <li class="nav-menu-item">
                        <a href="#about" class="nav-link">About</a>
                    </li>
                    <li class="nav-menu-item">
                        <a href="#contact" class="nav-link">Contact</a>
                    </li>
                </ul>

                <div class="search-wrapper">
                    <input type="text" class="search-input" placeholder="Search doctors, specialties..." />
                    <button class="search-button">Search</button>
                </div>

                <ul class="nav-auth-list">
                    @if (SignInManager.IsSignedIn(User))
                    {
                        <li class="nav-auth-item">
                            @{
                                // Decide which dashboard to send the user to, based on their role
                                var dashboardController = "Home";

                                if (User.IsInRole("Admin"))
                                {
                                    dashboardController = "Admin";
                                }
                                else if (User.IsInRole("Doctor"))
                                {
                                    dashboardController = "Doctor";
                                }
                                else if (User.IsInRole("Receptionist"))
                                {
                                    dashboardController = "Receptionist";
                                }
                                else if (User.IsInRole("Patient"))
                                {
                                    dashboardController = "Patient";
                                }
                            }
                            <a asp-controller="@dashboardController" asp-action="Dashboard" class="auth-link">
                                Dashboard
                            </a>
                        </li>
                        <li class="nav-auth-item">
                            <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                                <button type="submit" class="auth-link auth-link-button" style="background:none;border:none;">
                                    Logout
                                </button>
                            </form>
                        </li>
                    }
                    else
                    {
                        <!-- These buttons open the popup modal -->
                        <li class="nav-auth-item">
                            <button type="button" class="auth-link auth-link-button" id="navLoginBtn">Login</button>
                        </li>
                        <li class="nav-auth-item">
                            <button type="button" class="auth-link auth-link-button" id="navRegisterBtn">Register</button>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </nav>

    <!-- ===== PAGE CONTENT ===== -->
    <main role="main">
        @RenderBody()
    </main>

    <!-- ===== AUTH MODAL OVERLAY (Login + Register) ===== -->
    <div id="authOverlay" class="auth-overlay hidden">
        <div class="auth-overlay-backdrop"></div>

        <div class="auth-modal-wrapper">
            <!-- LOGIN CARD -->
            <div class="auth-modal-card auth-login-card" id="loginModal">
                <button type="button" class="auth-close-btn" id="authCloseBtn">&times;</button>

                <h2 class="auth-title">Welcome Back</h2>
                <p class="auth-subtitle">Login to manage your doctor appointments</p>

                @if (TempData["LoginError"] != null)
                {
                    <div class="auth-alert auth-alert-error">
                        @TempData["LoginError"]
                    </div>
                }

                <form asp-controller="Account" asp-action="Login" method="post" novalidate>
                    <div class="mb-3">
                        <label class="form-label">Email address</label>
                        <input name="Email" type="email" class="form-control auth-input" placeholder="Enter email" />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Password</label>
                        <input name="Password" type="password" class="form-control auth-input" placeholder="Enter password" />
                    </div>

                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <div class="form-check">
                            <input name="RememberMe" class="form-check-input" type="checkbox" id="rememberMeCheckbox" />
                            <label class="form-check-label" for="rememberMeCheckbox">
                                Remember me
                            </label>
                        </div>
                        <a href="#" class="auth-link-inline">Forgot password?</a>
                    </div>

                    <button type="submit" class="auth-primary-btn">Login</button>

                    <div class="auth-bottom-text">
                        Don’t have an account?
                        <button type="button" class="auth-link-inline auth-switch-btn" data-target="register">
                            Register here
                        </button>
                    </div>
                </form>
            </div>

            <!-- REGISTER CARD -->
            <div class="auth-modal-card auth-register-card hidden" id="registerModal">
                <button type="button" class="auth-close-btn" id="authCloseBtn2">&times;</button>

                <h2 class="auth-title">Create Account</h2>
                <p class="auth-subtitle">Register to book and manage your doctor appointments</p>

                @if (TempData["RegisterError"] != null)
                {
                    <div class="auth-alert auth-alert-error">
                        @TempData["RegisterError"]
                    </div>
                }

                <form asp-controller="Account" asp-action="Register" method="post" novalidate>
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label">First name</label>
                            <input name="FirstName" class="form-control auth-input" placeholder="Enter first name" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Last name</label>
                            <input name="LastName" class="form-control auth-input" placeholder="Enter last name" />
                        </div>

                        <div class="col-12">
                            <label class="form-label">Email address</label>
                            <input name="Email" type="email" class="form-control auth-input" placeholder="example@domain.com" />
                            <small class="text-muted">Use a valid email. It will be used for login and notifications.</small>
                        </div>

                        <div class="col-12">
                            <label class="form-label">Mobile number</label>
                            <input name="PhoneNumber" class="form-control auth-input" placeholder="e.g. 01XXXXXXXXX" />
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Date of birth</label>
                            <input name="DateOfBirth" type="date" class="form-control auth-input" />
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Gender</label>
                            <select name="Gender" class="form-select auth-input">
                                <option value="">Select gender</option>
                                <option>Male</option>
                                <option>Female</option>
                                <option>Other</option>
                            </select>
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Password</label>
                            <input name="Password" type="password" class="form-control auth-input" placeholder="Enter password" />
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Confirm password</label>
                            <input name="ConfirmPassword" type="password" class="form-control auth-input" placeholder="Confirm password" />
                        </div>

                        <div class="col-12">
                            <div class="form-check">
                                <input name="AcceptTerms" type="checkbox" class="form-check-input" id="acceptTermsCheckbox" value="true" />
                                <label class="form-check-label" for="acceptTermsCheckbox">
                                    I agree to the terms &amp; conditions and privacy policy.
                                </label>
                            </div>
                        </div>

                        <div class="col-12">
                            <button type="submit" class="auth-primary-btn">Create Account</button>
                        </div>
                    </div>

                    <div class="auth-bottom-text">
                        Already have an account?
                        <button type="button" class="auth-link-inline auth-switch-btn" data-target="login">
                            Login here
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- ===== Footer ===== -->
    <footer class="footer-section">
        <div class="footer-content">
            <h3 class="footer-title">🏥 HealthCare Appointments</h3>
            <p class="footer-description">Booking appointments with Sunshine Hospital made simple.</p>
            <p class="footer-copyright">
                &copy; @DateTime.UtcNow.Year Doctor Appointment System. All rights reserved.<br />
                Designed for responsive viewing on mobile, tablet and desktop.
            </p>
        </div>
    </footer>

    <!-- Bootstrap JS bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Site JS -->
    <script src="~/js/home.js" asp-append-version="true"></script>

    @RenderSection("Scripts", required: false)
</body>
</html>

